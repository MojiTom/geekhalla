import React, { Component }  from 'react';
import { NavLink } from 'react-router-dom';

class Choices extends Component {

    render () { 
        return (
            <div>
                Choices
            </div>
        );
    }
}

export default Choices; // React has other uses for "menu," so Choices seemed logical.